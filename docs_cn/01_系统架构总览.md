# 01 系统架构总览

## 1.1 设计目标

Claw Agent 是一个运行在资源受限嵌入式设备（ESP32）上的 AI Agent 框架，其核心设计目标：

1. **以 LLM 为决策中枢**：所有智能逻辑由 LLM 推理，框架只负责上下文组装、工具路由和结果传递。
2. **可插拔能力（Capability）体系**：任何功能以"能力"形式注册，LLM 可选择性地调用。
3. **基于事件的外部输入**：IM 消息、定时触发、传感器事件通过统一的事件路由层进入系统。
4. **持久化记忆**：跨会话的长期记忆，支持结构化存储、摘要索引和自动提取。
5. **技能（Skill）动态激活**：能力集合可以按会话级别动态启用/禁用，通过技能文档描述元数据。

---

## 1.2 宏观架构图

```mermaid
graph TB
    subgraph 外部输入
        IM[IM 平台<br/>TG/WeChat/QQ/飞书]
        SCH[调度器<br/>Cron/Interval/Once]
        EXT[其他事件源<br/>传感器/HTTP等]
    end

    subgraph 事件路由层 claw_event_router
        QUEUE[事件队列<br/>event_queue]
        RULES[规则引擎<br/>rule_list]
        QUEUE --> RULES
    end

    subgraph Claw Agent 核心
        CORE[claw_core<br/>Agent 主循环]
        CAP[claw_cap<br/>能力注册表]
        SKILL[claw_skill<br/>技能注册表]
        MEM[claw_memory<br/>长期记忆]
        LLM[LLM 后端<br/>OpenAI/Anthropic/Custom]
    end

    subgraph 持久化存储
        FS[文件系统<br/>FATFS / RAMFS]
        SESSION[会话历史<br/>session/*.jsonl]
        MEMDB[记忆数据库<br/>memory_records.jsonl<br/>memory_index.json]
        SKILLST[技能状态<br/>skill_state/*.json]
    end

    IM -->|publish_message| QUEUE
    SCH -->|publish_trigger| QUEUE
    EXT -->|publish_event| QUEUE

    RULES -->|run_agent: submit request| CORE
    RULES -->|call_cap: 直接调用| CAP
    RULES -->|send_message: 发送回复| IM

    CORE -->|收集 context providers| MEM
    CORE -->|收集 context providers| SKILL
    CORE -->|收集 context providers| CAP
    CORE -->|HTTP 请求| LLM
    CORE -->|调用工具| CAP
    CORE -->|persist_session| SESSION

    CAP -->|执行| IM
    CAP -->|执行| FS
    CAP -->|激活技能| SKILL

    MEM <-->|读写| MEMDB
    SKILL <-->|读写| SKILLST
    CORE -->|会话历史注入| SESSION
```

---

## 1.3 核心数据流

### 1.3.1 外部消息 → Agent 响应

```mermaid
sequenceDiagram
    participant User as 用户(IM/定时器)
    participant Router as 事件路由器
    participant Core as claw_core
    participant LLM as LLM 服务
    participant Cap as 能力系统
    participant Mem as 记忆系统

    User->>Router: 发布事件(event)
    Router->>Router: 匹配规则 run_agent
    Router->>Core: claw_core_submit(request)
    Core->>Mem: 收集会话历史
    Core->>Mem: 收集长期记忆摘要
    Core->>Cap: 收集可用工具列表
    Core->>LLM: POST /chat/completions
    LLM-->>Core: 响应 (text 或 tool_calls)
    alt 有工具调用
        loop 最多 max_tool_iterations 次
            Core->>Cap: claw_core_call_cap(tool_name, args)
            Cap-->>Core: tool_output
            Core->>LLM: 携带工具结果再次调用
            LLM-->>Core: 新响应
        end
    end
    Core->>Mem: 持久化会话记录
    Core->>Router: 发布 out_message 事件
    Router->>User: 发送回复
```

### 1.3.2 系统初始化序列

```mermaid
sequenceDiagram
    participant App as 应用层(app_claw)
    participant Ramfs as claw_ramfs
    participant Mem as claw_memory
    participant Skill as claw_skill
    participant Cap as claw_cap
    participant Router as claw_event_router
    participant Core as claw_core
    participant CapImpls as 各 Capability 实现

    App->>Ramfs: claw_ramfs_register(RAMFS挂载点)
    App->>Mem: claw_memory_init(config)
    App->>Skill: claw_skill_init(config)
    App->>Cap: claw_cap_init()
    App->>CapImpls: cap_xxx_register_group()
    App->>Cap: claw_cap_set_llm_visible_groups(groups)
    App->>Core: claw_core_init(config)
    App->>Core: claw_core_add_context_provider(memory_provider)
    App->>Core: claw_core_add_context_provider(skill_provider)
    App->>Core: claw_core_add_context_provider(cap_tools_provider)
    App->>Router: claw_event_router_init(config)
    App->>Core: claw_core_start()
    App->>Router: claw_event_router_start()
    Router->>App: 发布 startup/boot_completed 触发事件
```

---

## 1.4 分层架构

| 层次 | 组件 | 职责 |
|------|------|------|
| **应用层** | app_claw | 配置、初始化、连接所有组件 |
| **协调层** | claw_event_router | 事件分发、规则匹配、行为编排 |
| **Agent 层** | claw_core | LLM 调用主循环、上下文构建、工具调度 |
| **能力层** | claw_cap | 工具注册、分组管理、LLM 可见性控制 |
| **认知层** | claw_memory | 长期记忆存储、检索、自动提取 |
| **技能层** | claw_skill | 技能文档目录、会话级能力激活 |
| **LLM 适配层** | claw_core/llm/ | 多后端抽象、HTTP 传输、媒体处理 |
| **基础设施层** | claw_ramfs | RAM 文件系统（移植时替换） |

---

## 1.5 关键设计约束

### 单线程 Agent 主循环
- `claw_core` 有且仅有一个工作任务（`claw_core_task`）。
- 同一时刻只能处理一个请求（串行执行）。
- 请求通过 FreeRTOS 队列排队，顺序处理。

### 用户中断机制（User Interrupt）
- 当 Agent 正在处理请求时，同一会话（同 session_id）的新消息可以通过 `CLAW_CORE_REQUEST_FLAG_USER_INTERRUPT` 标志"插入"（Insert Queue）。
- 如果 LLM HTTP 请求正在进行，会触发 HTTP 中止（abort），然后将新消息注入当前消息链重新推理。

### 工具迭代上限
- 每个请求的工具调用轮次有上限 `max_tool_iterations`（默认 10）。
- 超过上限时返回错误"cap tool iteration limit reached"。

### 上下文提供者（Context Provider）机制
- Agent 不直接持有所有上下文，而是通过注册的 `context_provider` 函数在每轮推理前动态收集。
- `REQUEST_START_ONLY` 标志的 provider 只在请求开始时收集一次并缓存，避免会话历史在每轮工具调用中重复加载。

### 能力可见性分层
- 全局可见性：`claw_cap_set_llm_visible_groups()` 设置所有会话默认可见的能力组。
- 会话级可见性：`claw_cap_set_session_llm_visible_groups()` 覆盖特定会话的可见能力组。
- Skill 激活：通过 `cap_skill_mgr` 为特定会话激活 Skill，间接解锁对应的能力组。

---

## 1.6 模块依赖关系

```mermaid
graph LR
    claw_core --> claw_cap
    claw_core --> claw_event_router
    claw_core --> claw_skill
    claw_core --> claw_memory
    claw_event_router --> claw_cap
    claw_event_router --> claw_core
    claw_cap --> claw_core
    cap_skill_mgr --> claw_skill
    cap_skill_mgr --> claw_cap
    cap_session_mgr --> claw_memory
    cap_session_mgr --> claw_skill
    cap_scheduler --> claw_event_router
    cap_im_platform --> claw_event_router
    claw_memory --> claw_core
    claw_skill -.-> claw_cap
```

依赖说明：
- `→` 表示强依赖（编译时链接）
- `-.→` 表示逻辑依赖（运行时知道 group_id）
