Revise a remembered fact with `memory.update`, identified by the `id` shown in
brackets by `memory.recall` / `memory.list`. Provide only the fields you want to
change — content, tags, or keywords; the rest stay as they were.
