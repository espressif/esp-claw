List everything you currently remember with `memory.list`, newest first. Each
entry shows its id in brackets so you can pass it to `memory.update` or
`memory.forget`. Prefer `memory.recall` when you already know the topic.
