Delete one of your subagents by id, removing it and everything it spawned. You
can only delete a subagent in your own subtree. Use it to clean up a persistent
(`manual`) subagent once you no longer need it; one-shot (`auto`) subagents
remove themselves and do not need this.
