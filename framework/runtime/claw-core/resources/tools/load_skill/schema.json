{
  "type": "function",
  "function": {
    "name": "load_skill",
    "description": "Load one skill's guidance into your context for the rest of the conversation. Pass the skill id from list_skills. Use this when a task needs specialized instructions you do not already have loaded.",
    "parameters": {
      "type": "object",
      "properties": {
        "skill": {
          "type": "string",
          "description": "The id of the skill to load (as shown by list_skills)."
        }
      },
      "required": ["skill"]
    }
  }
}
