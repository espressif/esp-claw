Recall what you remembered earlier with `memory.recall`. Pass one or more `labels`
from your memory topics to narrow by subject, and/or a `query` to filter by text.
Each result shows an id in brackets (e.g. `[g-3]`) that you pass to
`memory.update` or `memory.forget` to edit or remove it.
