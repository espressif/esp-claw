Remember a durable fact with `memory_store`. Reserve it for information worth
recalling across future conversations — user preferences, identity details,
standing commitments — not one-off task state. Give each memory one or more short
`tags`; you later recall everything under a tag with `memory_recall`. Storing a
fact you already remember is a no-op, so you do not need to check first.
