{
  "type": "function",
  "function": {
    "name": "unload_skill",
    "description": "Drop one previously loaded skill from your context to free space when its guidance is no longer needed. Pass the skill id.",
    "parameters": {
      "type": "object",
      "properties": {
        "skill": {
          "type": "string",
          "description": "The id of the loaded skill to unload."
        }
      },
      "required": ["skill"]
    }
  }
}
