{
  "type": "function",
  "function": {
    "name": "skill.reload",
    "description": "Re-scan the skills directories from disk so skills installed, edited, or removed at runtime become visible. Call this once after skills change on disk, then use skill.list or skill.activate.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    }
  }
}
