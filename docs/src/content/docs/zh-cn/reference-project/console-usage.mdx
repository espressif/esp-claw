---
title: Console 交互
description: 串口调试 Console 当前状态。
---

旧的 C `app_claw` 串口 CLI 已经从 `applications/shared/app_claw` 移除。

替代实现位于 Rust 侧的 `framework/services/debug-console` 和
`applications/shared/app-claw`，但目前还没有接入固件 runtime。在完成接入前，
`edge_agent` 默认不会启动 `app_claw` 命令 REPL。
